<h1>id: 
<%= @task.id %>
 のタスク編集ページ</h1>
 
 <%= reder 'form', task: @task %>
 
 <%= link_to '一覧に戻る', tasks_path %>

<%= form_with(model: @task, local: true) do |f| %>

  
<%= f.label :content, 'タスク' %>

  
<%= f.text_field :content %>


  
<%= f.submit '投稿' %>

<% end %>


<%= link_to '一覧に戻る', task_path %>